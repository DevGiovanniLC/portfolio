---
import "../styles/global.css";
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta http-equiv="Content-Language" content="es" />

		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="robots" content="index, follow" />

		<meta name="author" content="Giovanni León Corujo" />
		<meta
			name="description"
			content="Portfolio de Giovanni León Corujo - Ingeniero informático | Desarrollador Full Stack | QA Tester"
		/>
		<meta
			name="keywords"
			content="Portfolio, Giovanni León Corujo, Ingeniero informático, Desarrollador Full Stack, QA Tester, Busqueda de empleo"
		/>

		<meta property="og:title" content="Portfolio - Giovanni León Corujo" />
		<meta
			property="og:description"
			content="Ingeniero informático | Desarrollador Full Stack | QA Tester"
		/>
		<meta property="og:type" content="website" />
		<meta
			property="og:url"
			content="https://portfolio-giovanni-lc.vercel.app/"
		/>
		<meta
			property="og:image"
			content="https://portfolio-giovanni-lc.vercel.app/og-image.png"
		/>

		<meta name="generator" content={Astro.generator} />
		<link rel="icon" type="image/png" href="/favicon.png" />
		<title>Portfolio - Giovanni</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	html,
	body {
		display: grid;
		margin: 0;
		padding: 0;
		width: 100%;
		min-height: 100vh;
		grid-template-rows: auto 1fr auto;
		background-image:
			radial-gradient(
				circle at 20% 50%,
				rgba(34, 34, 34, 0.652) 0%,
				transparent 50%
			),
			radial-gradient(
				circle at 80% 20%,
				rgba(29, 29, 29, 0.816) 0%,
				transparent 50%
			),
			radial-gradient(
				circle at 40% 80%,
				rgba(7, 7, 7, 0.717) 0%,
				transparent 50%
			),
			linear-gradient(135deg, #2c2c2c 0%, #101010 50%, #252525 100%);
		background-color: #1f1f1f;
		background-size: cover;
		background-position: center;
		background-attachment: fixed;
		backdrop-filter: blur(10px);
		font-family: "Inter", sans-serif;
	}
</style>

<script>
	import Lenis from "lenis";

	// Detectar si tiene soporte táctil
	const isTouchDevice = window.matchMedia("(pointer: coarse)").matches;

	const lenis = new Lenis({
		duration: 1.2,
		easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
		wheelMultiplier: 1,
		touchMultiplier: 2,
	});

	function raf(time: number) {
		lenis.raf(time);
		requestAnimationFrame(raf);
	}

	requestAnimationFrame(raf);

	// Smooth scroll para enlaces ancla
	document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
		anchor.addEventListener("click", (e) => {
			e.preventDefault();
			const targetId = (
				e.currentTarget as HTMLAnchorElement
			).getAttribute("href");

			if (targetId) {
				const targetElement = document.querySelector(targetId);
				if (targetElement) {
					// Calcular distancia para ajustar duración
					const currentScroll = lenis.scroll;
					const targetScroll =
						targetElement.getBoundingClientRect().top +
						currentScroll -
						100;
					const distance = Math.abs(targetScroll - currentScroll);

					// Duración proporcional a la distancia (mínimo 1s, máximo 3s)
					const dynamicDuration = Math.min(
						Math.max(distance / 1000, 1),
						3,
					);

					lenis.scrollTo(targetElement as HTMLElement, {
						offset: -100,
						duration: dynamicDuration,
						easing: (t: number) => {
							// Ease-out exponencial - Desaceleración muy fuerte y homogénea
							return 1 - Math.pow(1 - t, 5);
						},
					});
				}
			}
		});
	});
</script>
